==============
	IoT 인줄 알았던 ...
	python programming extended.
==============

* env 세팅을 위해... start from scratch


요즘 대세는, uv 를 활용한 (still no 1.0 release, iterating on base 0.x versions, but super good so widely begin used)
- rust-based environment settings for python
(옛날에는 아나콘다니 venv니, 세팅이 묶이는 경향이 있었음. then enter uv.

```
uv run main.py
uv pip install package_name
```

easier!

그래서 새로 업데이트된 pwsh (powershell v 7) 설치, vs code 설치, python 재설치 등. 진행하는 중


** uv add ....
- only add packages after you activate venv !

uv add ...
uv init <folder_name> python=3.14
uv activate
uv run x.py

======
Getting Into MLOps / Cloud Infra
======

- managing VMs for ML operations
- was debating WSL2 + Docker, and VMs
- so AWS EC2 are basically VMs...

so in terms of managing infra/equipment ? VM concepts are important.

for the context of this teaching module
- the user assignment and previleges may be a little mixed for WSL2
- using VM to spin up a linux environment
- in practice and development, Dockers are widely used
- fundamentals of SSH, terminal access to servers.
